# The class cannot be named by Pascal or camel case.
# The class cannot be named by Pascal or camel case.
# If it is not used, the corresponding structure will not be set,
# and it will not be read naturally.
serverversion: 2.0.0
#---------------BlockchainData configuration---------------------#
redis:
  # dbAddress: [ blockchaindata.ad9n4e.clustercfg.apse1.cache.amazonaws.com:6379 ]
  dbAddress: [ 10.211.55.5:16379 ] #redis地址 单机时，填写一个地址即可，使用redis集群时候，填写集群中多个节点地址（主从地址都可以填写，增加容灾能力），默认即可
  dbMaxIdle: 128
  dbMaxActive: 0
  dbIdleTimeout: 120
  dbPassWord: "" #redis密码 建议修改
  enableCluster: false #如果外部redis以集群方式启动，需要打开此开关
